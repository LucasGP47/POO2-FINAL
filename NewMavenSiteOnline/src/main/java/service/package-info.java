/**
 * Contém classes de serviço que implementam a lógica principal do monitoramento de sites.
 * <p>
 * Este pacote inclui várias classes de serviço, como `MonitoringService`, `SiteChecker`, `LogService` e `NotificationService`.
 * Estas classes são responsáveis por verificar o status dos sites, registrar logs, enviar notificações e outras
 * operações.
 * </p>
 */
package service;
