/**
 * Contém classes utilitárias usadas em várias partes do projeto.
 * <p>
 * Este pacote inclui classes como `HttpUtil` e `FileUtil`, que fornecem métodos auxiliares para
 * operações comuns, como leitura de arquivos e obtenção de respostas HTTP.
 * </p>
 */
package util;
